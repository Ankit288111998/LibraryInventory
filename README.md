# LibraryInventory
It is a simple java-spring boot project using rest api. It use to store, update, delete and retrieve book from a list. Database is not used but if you want code skelton is already written just uncomment it and start configuring.
